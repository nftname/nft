# NNM Market - NFT Marketplace Setup Guide

## 🎉 تم تكوين المشروع بنجاح!

تم تحويل مشروع Scaffold-ETH 2 إلى NFT Marketplace كامل يسمى **NNM Market** متصل بـ Polygon Mainnet.

---

## 📋 ما تم تنفيذه:

### ✅ 1. ملف البيئة (`.env.local`)

تم إنشاء ملف `.env.local` في `/workspaces/nft/packages/nextjs/` يحتوي على:

- Alchemy API Key لـ Polygon Mainnet
- WalletConnect Project ID
- عنوان العقد الذكي على Polygon
- Pinata JWT للرفع على IPFS
- Pinata Gateway URL

### ✅ 2. تكوين Scaffold (`scaffold.config.ts`)

- تم تحديث `targetNetworks` إلى `[chains.polygon]`
- تم تعطيل `onlyLocalBurnerWallet`

### ✅ 3. تكوين العقد الذكي (`contracts/deployedContracts.ts`)

- تم إضافة ABI للعقد الذكي على Polygon (Chain ID: 137)
- العقد متصل بالعنوان: `0x41d0d53f5a4aabe92f218f2088351e3a1b9f0cd9`
- يتضمن الدوال: `mint`, `mintPrice`, `tokenURI`, `balanceOf`, إلخ.

### ✅ 4. صفحات التطبيق:

#### 📄 `/mint` - صفحة السك (Minting)

- واجهة لإدخال اسم الـ NFT
- التحقق من سعر السك من العقد الذكي
- رفع Metadata إلى IPFS عبر Pinata
- استدعاء دالة `mint` على العقد الذكي

#### 📄 `/marketplace` - السوق

- عرض جميع الـ NFTs المسكوكة
- إحصائيات (Total Supply, Mint Price)
- عرض الـ NFTs في شبكة (Grid)

#### 📄 `/dashboard` - لوحة التحكم

- عرض NFTs المملوكة للمستخدم المتصل
- عدد الـ NFTs المملوكة
- روابط لعرض Metadata

#### 📄 `/` - الصفحة الرئيسية

- تصميم جديد يعرض ميزات NNM Market
- أزرار للانتقال إلى Mint, Marketplace, Dashboard
- معلومات عن المزايا (Polygon, IPFS, إلخ)

### ✅ 5. API Route (`/api/mint/route.ts`)

- استقبال اسم الـ NFT من الـ Frontend
- إنشاء صورة SVG بالاسم
- رفع الصورة إلى IPFS عبر Pinata
- إنشاء ملف Metadata JSON
- رفع Metadata إلى IPFS
- إرجاع `tokenURI` للـ Frontend

### ✅ 6. التنقل (`Header.tsx`)

- تم تحديث القائمة لتشمل: Home, Mint, Marketplace, Dashboard

---

## 🚀 كيفية التشغيل:

### 1. التأكد من التبعيات

جميع التبعيات المطلوبة موجودة بالفعل في المشروع! لا حاجة لتثبيت حزم إضافية.

التبعيات الرئيسية المستخدمة:

- `next` - Next.js framework
- `wagmi` - للتفاعل مع Ethereum/Polygon
- `@rainbow-me/rainbowkit` - للاتصال بالمحفظة
- `viem` - للتفاعل مع البلوكتشين
- Native `fetch` API - للتفاعل مع Pinata API

### 2. التحقق من ملف البيئة

تأكد من أن ملف `.env.local` موجود في `packages/nextjs/`:
\`\`\`bash
cd /workspaces/nft/packages/nextjs
ls -la .env.local
\`\`\`

### 3. تشغيل المشروع

\`\`\`bash
cd /workspaces/nft
yarn install # إذا لزم الأمر
cd packages/nextjs
yarn start
\`\`\`

أو من الجذر:
\`\`\`bash
cd /workspaces/nft
yarn start
\`\`\`

### 4. فتح المتصفح

افتح: `http://localhost:3000`

---

## 🔧 الاستخدام:

### سك NFT جديد:

1. اذهب إلى `/mint`
2. صل محفظتك (تأكد من اتصالك بـ Polygon Mainnet)
3. أدخل اسماً فريداً للـ NFT
4. اضغط "Mint NFT"
5. وافق على المعاملة في محفظتك (ستدفع حوالي $10-$50 بـ POL)

### تصفح السوق:

1. اذهب إلى `/marketplace`
2. شاهد جميع الـ NFTs المسكوكة على NNM Market

### لوحة التحكم:

1. اذهب إلى `/dashboard`
2. صل محفظتك لرؤية NFTs الخاصة بك

---

## 🛠️ ملاحظات تقنية:

### العقد الذكي:

- **العنوان**: `0x41d0d53f5a4aabe92f218f2088351e3a1b9f0cd9`
- **الشبكة**: Polygon Mainnet (Chain ID: 137)
- **الدالة الرئيسية**: `mint(string tokenURI) payable`

### IPFS & Pinata:

- يتم رفع الصور والـ Metadata إلى IPFS عبر Pinata API
- الصور تُنشأ ديناميكياً كـ SVG
- Metadata بصيغة JSON قياسية لـ NFT

### Wagmi Hooks المستخدمة:

- `useScaffoldReadContract` - لقراءة بيانات العقد (mintPrice, balance, إلخ)
- `useScaffoldWriteContract` - لاستدعاء دالة mint
- `useAccount` - للحصول على عنوان المحفظة المتصلة

---

## 📦 لا توجد حزم إضافية مطلوبة!

المشروع يستخدم:

- ✅ Native Fetch API للتواصل مع Pinata (لا حاجة لـ axios أو pinata-web3)
- ✅ wagmi & viem للتفاعل مع البلوكتشين
- ✅ جميع التبعيات موجودة في package.json الأصلي

---

## 🎯 الخطوات التالية (اختيارية):

1. **تحسين Marketplace**: إضافة فلترة وبحث
2. **تحسين Dashboard**: إضافة إمكانية نقل NFTs
3. **التكامل الفعلي مع العقد**:
   - استدعاء `tokenOfOwnerByIndex` لجلب NFTs المملوكة
   - استدعاء `tokenByIndex` لجلب جميع NFTs
   - جلب Metadata من IPFS
4. **معالجة الأخطاء**: إضافة معالجة أفضل للأخطاء
5. **UI/UX**: تحسين التصميم وإضافة animations

---

## 🚨 تذكير مهم:

- تأكد من اتصالك بـ **Polygon Mainnet** وليس Hardhat local
- تحتاج إلى POL في محفظتك لدفع رسوم Gas
- سعر السك يتم تحديده من العقد الذكي عبر دالة `mintPrice()`

---

## 📞 الدعم:

إذا واجهت أي مشاكل:

1. تحقق من Console في المتصفح
2. تحقق من logs في Terminal
3. تأكد من ملف `.env.local` يحتوي على القيم الصحيحة
4. تأكد من اتصالك بـ Polygon Mainnet

---

**تم بناؤه باستخدام Scaffold-ETH 2 ❤️**
